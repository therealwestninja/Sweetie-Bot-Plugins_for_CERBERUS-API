# upstream-api

This directory represents the intended integration surface for the upstream robot runtime/API.
In a real build, this directory would be synchronized with the actual upstream project or included as a submodule/subtree.

For the scaffold release, it contains placeholders only.
