[project]
name = "sweetiebot-fork"
version = "0.0.1"
description = "Aspirational monorepo scaffold for a character robot fork"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
