# upstream-web

This directory represents the intended integration surface for the upstream operator web interface.
In a real build, this would be synchronized with the actual upstream web project.

For the scaffold release, it contains placeholders and mock panels only.
