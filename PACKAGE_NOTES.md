# Package Notes

This packaged repo copy adds a design and contract layer on top of the existing project tree.

Main additions:
- `Docs/` for human-readable architecture documents
- `schemas/` for machine-readable JSON Schemas
- `shared/sweetie_plugin_sdk/` for a shared contract SDK
- `controller/` for a controller skeleton
- `templates/reference-plugin/` for a standard plugin starter
